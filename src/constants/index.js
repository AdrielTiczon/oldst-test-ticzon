/*
  Normally this API_BASE_URL would be in a .env file
  In this case I did not set that up and just opt to put it here.
*/

export const API_BASE_URL = 'http://localhost:8000';

export default API_BASE_URL;
